/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  //   basePath: '/turnami-landing',
  //   trailingSlash: true,
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
